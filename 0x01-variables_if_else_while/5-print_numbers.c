#include <stdio.h>
/* more headers goes there */
/**
 * main - Entry point
 *
 * Description: 'Numbers'
 *
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (i = 0 ; i > 10 ; i++)
		printf("%d", i);
	return (0);
}
